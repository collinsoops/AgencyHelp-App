class Menu {
  const Menu(
      {required this.title,
        required this.description,
        required this.strImages,
        required this.thesteps});
  final String title;
  final String description;
  final List<String> strImages;
  final List<String> thesteps;
}